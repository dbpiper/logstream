log_groups = ["/aws/lambda/example"]        # required: set via env LOG_GROUPS or here
region = ""                                 # required: set via env AWS_REGION or here
index_prefix = "logs"                       # default; override with env INDEX_PREFIX
batch_size = 100
max_in_flight = 2
poll_interval_secs = 15
reconcile_interval_secs = 900
backfill_days = 3650
checkpoint_path = "/state/checkpoints.json"
http_timeout_secs = 30
backoff_base_ms = 200
backoff_max_ms = 10000
